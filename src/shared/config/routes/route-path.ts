export enum enumPath {
  HOME = "/",
  ABOUT = "/about",
  DELIVARY = "/delivery",
  STORES = "/stores",
  CONTACTS = "/contact",
  CATALOG = "/catalog",
  PRODUCT_CARD = "/product_card",
  CART = "/cart",
}
